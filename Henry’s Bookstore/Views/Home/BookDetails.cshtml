@model Henry_s_Bookstore.Models.BOOK

@{
    ViewBag.Title = Model.TITLE;
}
<div class="row gray-div">
    <p class="heading-position">For INTERNAL use only !</p>
</div>
<h4 class="text-center">@Model.TITLE.ToUpper()</h4>
<div class="row">
    <div class="col-3">
        @Html.Partial("_Menu");
    </div>
    <div class="col">
        <img src="~/Content/Images/@Model.BOOK_CODE/large.jpg" alt="@Model.TITLE" />
    </div>
    <div class="col">
        <table class="table table-bordered table-striped">
            <tbody>
                <tr>
                    <th>Title</th>
                    <td>@Model.TITLE</td>
                </tr>
                <tr>
                    <th>Type</th>
                    <td>@Model.TYPE</td>
                </tr>
                <tr>
                    <th>Price</th>
                    <td>$@Model.PRICE</td>
                </tr>
                <tr>
                    <th>Paperback?</th>

                    @if (Model.PAPERBACK == "1")
                    {
                        <td>True</td>
                    }
                    else
                    {
                        <td>False</td>
                    }
                </tr>
                <tr>
                    <th>Publisher</th>
                    <td>@Model.PUBLISHER.PUBLISHER_NAME</td>
                </tr>
                <tr>
                    <th>Written by</th>
                    <td>@ViewBag.Author.AUTHOR_FIRST @ViewBag.Author.AUTHOR_LAST</td>
                </tr>
            </tbody>
        </table>

        <h5>Inventory by Location</h5>
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th scope="col">Location</th>
                    <th scope="col">On Hand Quantity</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var location in ViewBag.Locations)
                {
                <tr>
                    <th>@location.BRANCH.BRANCH_NAME</th>
                    <td>@location.ON_HAND</td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>
